# День 29: Локальный аналитик 📊🕵️‍♂️

## Задание
Использовать локальную LLM для анализа данных проекта в офлайн-режиме.
В качестве источника данных выбраны **логи Git** текущего репозитория.

## Реализация
Приложение `Main.kt` трансформировано в аналитический чат-бот.

### Как это работает:
1.  **Сбор данных**: При запуске приложение выполняет команду `git log` через системный процесс.
2.  **Форматирование**: Логи преобразуются в чистый табличный вид (`Hash | Author | Date | Message`).
3.  **Контекст**: Последние 50 коммитов "скармливаются" модели `qwen2.5:1.5b` в системном промпте.
4.  **Анализ**: Вы задаете вопросы на естественном языке, а модель ищет ответы в предоставленных данных.

### Особенности:
- **Низкая температура (0.2)**: Для минимизации фантазий и максимизации точности подсчетов.
- **Интерактивность**: Можно задавать уточняющие вопросы ("А что именно он сделал в этом коммите?").
- **Приватность**: Ваши логи не уходят ни в какое облако, всё обрабатывается локально через Ollama.

## Как запустить

1.  Убедитесь, что вы находитесь в Git-репозитории (и у вас есть коммиты!).
2.  Запустите аналитика:
    ```bash
    ./gradlew run --console=plain
    ```
3.  Задавайте вопросы!

### Примеры вопросов:
- "Who is the most active contributor?"
- "What features were implemented in Day 25?"
- "List all commits related to README changes."
- "Did we fix any bugs recently?"

### Пример вывода:
```text
🔍 extracting git logs...
✅ Loaded 15 recent commits.

🤖 Analyst is ready! Ask questions like:
- 'Who made the most commits?'

You: Who worked on the README?
Analyst: According to the logs, Vera Skokova updated the README multiple times, specifically in commits `fae812f` (Day 28) and `37a015d` (Day 26).
```
