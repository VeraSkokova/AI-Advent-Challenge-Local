# День 25: Запустить локальную модель 🏠🤖

## Задание
В рамках адвент-челленджа нужно **запустить локальную языковую модель (LLM)** и интегрировать её в приложение на Kotlin.  
Цель: получить работающий чат-бот, который не зависит от внешних API (OpenAI/Yandex) и работает полностью автономно на машине пользователя.

## Что реализовано

### 1. Интеграция с Ollama
- Использован **Ktor Client (CIO)** для взаимодействия с локальным API Ollama (`http://localhost:11434`).
- Реализован кастомный клиент `OllamaClient` с поддержкой:
    - Увеличенных таймаутов (до 60 сек), так как локальные модели могут отвечать медленно.
    - Маппинга сообщений в формат чата (role/content).
    - Работы без стриминга (для простоты реализации).

### 2. Чат с памятью (Context Window Management)
- Приложение поддерживает **бесконечный диалог** в консоли.
- Реализована **история сообщений**: бот помнит контекст переписки.
- **Автоматическая суммаризация**:
    - Когда история превышает 10 сообщений, приложение автоматически сжимает старый контекст.
    - Для сжатия используется та же локальная модель.
    - Старые сообщения заменяются на одно системное сообщение с кратким содержанием ("Summary").

### 3. Логирование
- Приложение явно сообщает, с какой моделью работает (по умолчанию `qwen2.5:1.5b`).
- В консоль выводятся системные уведомления о статусе запроса и процессе сжатия истории.

## Инфраструктура

- **Язык**: Kotlin 2.2.20
- **Сборка**: Gradle Kotlin DSL
- **Библиотеки**:
    - `ktor-client-core`, `ktor-client-cio` — HTTP клиент.
    - `kotlinx.serialization` — JSON парсинг.
    - `kotlinx.coroutines` — асинхронность.

## Как запустить

### Шаг 1: Установка Ollama
1. Скачайте и установите [Ollama](https://ollama.com).
2. Выберите модель под ваше железо. Для GPU с 2-4GB VRAM рекомендуется **Qwen 2.5 1.5B**:
   ```bash
   ollama pull qwen2.5:1.5b
   ```
3. Запустите сервер (оставьте терминал открытым):
   ```bash
   ollama run qwen2.5:1.5b
   ```

### Шаг 2: Запуск приложения
Клонируйте репозиторий и запустите через Gradle:

```bash
git clone https://github.com/VeraSkokova/AI-Advent-Challenge-Local.git
cd AI-Advent-Challenge-Local
./gradlew run --console=plain
```

### Пример работы

```text
--- Day 25: Local LLM Chat (with History) ---

You: Привет, кто ты?

[System] Sending request to qwen2.5:1.5b...
AI: Привет! Я Qwen, большая языковая модель, созданная Alibaba Cloud. Чем могу помочь?

You: Расскажи про Kotlin в двух словах.

[System] Sending request to qwen2.5:1.5b...
AI: Kotlin — это современный, лаконичный и безопасный язык программирования, работающий на JVM.
```

## Полезные ссылки
- [Ollama GitHub](https://github.com/ollama/ollama)
- [Ktor Documentation](https://ktor.io/)
- [Qwen 2.5 Models](https://ollama.com/library/qwen2.5)
