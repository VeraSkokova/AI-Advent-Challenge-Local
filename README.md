# День 29: Локальный аналитик 📊🕵️‍♂️ (MCP Edition)

## Задание
Использовать локальную LLM для анализа данных проекта в офлайн-режиме.
Мы пошли дальше простого анализа и реализовали **автономного агента** с архитектурой **ReAct (Reason + Act)**.

## Реализация: MCP-Based ReAct Agent
Приложение трансформировано в интеллектуального помощника, который может **самостоятельно выбирать инструменты** для решения задач.

### Архитектура
1.  **LocalMCPServer**: Класс-регистр, управляющий доступными инструментами.
2.  **ReAct Loop**: Основной цикл в `Main.kt`, который:
    - Принимает запрос пользователя.
    - Позволяет модели "подумать" и выбрать инструмент (вывод JSON).
    - Выполняет инструмент (Git, файловая система).
    - Возвращает результат модели для финального ответа.

### Доступные инструменты (Tools)
Модель `qwen2.5:1.5b` теперь умеет использовать:
- `git_stats`: **NEW!** Получение точной статистики (общее число коммитов, топ авторов). Решает проблему "галлюцинаций" при подсчете.
- `git_log`: Чтение истории коммитов (можно задать лимит).
- `list_files`: Просмотр структуры проекта (ls).
- `read_file`: Чтение содержимого файлов (cat).

## Как запустить

1.  Убедитесь, что Ollama запущена: `ollama run qwen2.5:1.5b`
2.  Запустите агента:
    ```bash
    ./gradlew run --console=plain
    ```

### Примеры сложных запросов
Теперь вы можете задавать многосоставные вопросы, требующие исследования:

*   *"Who made the most commits?"*
    (Агент вызовет `git_stats` и вернет точную цифру, а не выдуманную)

*   *"Read build.gradle.kts and tell me what dependencies we use."*
    (Агент вызовет `read_file`, прочитает код и перечислит библиотеки)

*   *"List files in src/main/kotlin and read the first one."*
    (Агент вызовет `list_files`, выберет файл и вызовет `read_file`)

## Технические особенности
- **JSON Tool Calling**: Мы научили текстовую модель вызывать функции через строгий JSON-формат.
- **Robust Parsing**: Реализована защита от некорректного JSON (например, массивов вместо строк), свойственного маленьким моделям.
- **Path Correction**: В системный промпт вшиты правила защиты от "галлюцинаций" путей.
- **Приватность**: Весь анализ кода и истории Git происходит строго локально.
