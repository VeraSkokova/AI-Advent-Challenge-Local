# День 28: Оптимизация и адаптация локальной LLM 🧪⚙️

## Задание
Провести эксперименты с параметрами локальной модели (температура, контекст) и промпт-инжинирингом.
Сравнить "базовую" и "оптимизированную" версии генерации.

## Реализация: Benchmark Tool
Приложение модифицировано для запуска в режиме сравнения (A/B testing). При одном запуске оно генерирует два варианта документации.

### Сценарий 1: Baseline (Базовый)
- **Промпт**: Простой ("Generate a README.md...").
- **Температура**: `0.7` (стандартная, более креативная/хаотичная).
- **Контекст**: `2048` токенов.
- **Ожидание**: Может упустить детали, структура может плавать.

### Сценарий 2: Optimized (Оптимизированный)
- **Промпт**: Role-play ("You are a Senior Technical Writer..."), четкая структура, требования к стилю.
- **Температура**: `0.2` (низкая, для фактов и документации).
- **Контекст**: `4096` токенов (чтобы вместить больше кода без обрезки).
- **Ожидание**: Строгая структура, технически точный текст, профессиональный стиль.

## Как запустить

1. Убедитесь, что Ollama запущена: `ollama run qwen2.5:1.5b`
2. Запустите бенчмарк (указав путь к проекту):
   ```bash
   ./gradlew run --args="."
   ```

## Результаты
После запуска появятся два файла:
- `README_BASELINE.md`
- `README_OPTIMIZED.md`

А в консоли будет выведен отчет о времени выполнения:
```text
📊 BENCHMARK RESULTS:
Baseline:  15432ms | Temp: 0.7 | Context: 2048
Optimized: 18210ms | Temp: 0.2 | Context: 4096
```

## 🏆 Выводы по эксперименту
На основе анализа сгенерированных файлов (`README_BASELINE.md` vs `README_OPTIMIZED.md`) мы получили следующие результаты:

### 1. Влияние температуры (0.7 vs 0.2)
- **Baseline (0.7)**: Модель склонна к "галлюцинациям" структуры. Вместо того чтобы проанализировать код, она часто вставляет его кусками прямо в текст или создает абстрактные заголовки (`Feature 1`, `[Project Name]`), ожидая, что пользователь сам их заполнит.
- **Optimized (0.2)**: Модель строго придерживается фактов. Она извлекла реальное название проекта, корректно определила стек технологий и не придумала несуществующих функций.

### 2. Роль Промпта (Context & Persona)
- **Простой промпт**: Дал слабый результат. Модель не поняла, в каком стиле писать, и выдала "сырой" черновик.
- **Role-Play ("Senior Technical Writer")**: Кардинально улучшил качество. Текст стал структурированным, появились разделы "Getting Started" и "Tech Stack", оформленные профессионально.

### 3. Контекстное окно (2048 vs 4096)
- Увеличение контекста позволило передать модели больше исходного кода без обрезки. Это дало ей возможность "понять" проект целиком, а не гадать по первым строкам файлов.

### Итог
Для задач генерации документации и работы с кодом **критически важно**:
✅ Использовать **низкую температуру** (0.1–0.2).
✅ Задавать **четкую роль** и структуру ответа в промпте.
✅ Обеспечивать достаточный **размер контекста**.
